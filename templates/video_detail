<!DOCTYPE html>
<html>
<head>
    <title>Video Details</title>
</head>
<body>
    <h2>{{ video[1] }}</h2>
    <p>{{ video[2] }}</p>
    <video width="400" controls>
        <source src="/{{ video[8] }}" type="video/mp4">
        Your browser does not support the video tag.
    </video>
    <p>Publisher: {{ video[3] }}, Producer: {{ video[4] }}, Genre: {{ video[6] }}, Age Rating: {{ video[7] }}</p>
    <p>Average Rating: {{ avg_rating if avg_rating else 'No ratings yet' }}</p>
    <form method="post" action="{{ url_for('rate', video_id=video[0]) }}">
        Rate this video (1-5): <input type="number" name="rating" min="1" max="5" required>
        <input type="submit" value="Rate">
    </form>
    <h3>Comments</h3>
    <ul>
        {% for comment in comments %}
        <li>{{ comment[2] }}: {{ comment[3] }}</li>
        {% endfor %}
    </ul>
    <form method="post" action="{{ url_for('comment', video_id=video[0]) }}">
        <textarea name="content" required></textarea><br>
        <input type="submit" value="Comment">
    </form>
    <a href="{{ url_for('dashboard') }}">Back to Dashboard</a>
</body>
</html>
